<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jando.fitness_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ForgotPassword" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginActivity" />

        <activity
            android:name=".HomeScreen"
            android:label="@string/activity_home_screen" />
        <activity
            android:name=".ResourcesActivity"
            android:label="@string/activity_resources_label"/>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/activity_settings_label"/>
        <activity
            android:name=".ExerciseActivity"
            android:label="@string/activity_exercise_label" />
        <activity
            android:name=".NutritionActivity"
            android:label="@string/activity_nutrition_label" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>